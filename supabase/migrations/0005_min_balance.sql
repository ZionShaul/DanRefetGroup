-- ============================================================
-- סף יתרה מינימלי לתצוגה: מציגים ומסכמים רק שורות עם יתרה גדולה מהסף.
-- ניתן לעדכון במסך הגדרות מערכת. ברירת מחדל: 14 טון.
-- מיגרציה אדיטיבית (ניתן להריץ על DB קיים).
-- ============================================================

alter table system_settings add column if not exists min_balance numeric not null default 14;
